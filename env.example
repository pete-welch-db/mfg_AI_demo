# DENSO Operations Demo – environment variables
# Copy to .env and fill in values (or set in app/app.yaml / job env / CI).
# .env is gitignored; use scripts/update_env_ids.sh after deploy to push Genie/dashboard IDs into .env and app.yaml.

# -----------------------------------------------------------------------------
# App (Streamlit) – required for live data
# -----------------------------------------------------------------------------
DATABRICKS_HOST=https://adb-984752964297111.11.azuredatabricks.net
DATABRICKS_HTTP_PATH=/sql/1.0/warehouses/148ccb90800933a1
DATABRICKS_CATALOG=welch
DATABRICKS_SCHEMA=denso_ai

# Optional: use when app runs outside Databricks (e.g. local). In Databricks App, OAuth is used if unset.
# DATABRICKS_TOKEN=

# -----------------------------------------------------------------------------
# Genie (post-deploy: run scripts/deploy_genie_space.py, then update_env_ids.sh)
# -----------------------------------------------------------------------------
GENIE_SPACE_ID=
GENIE_PLANNING_SPACE_ID=

# -----------------------------------------------------------------------------
# Optional
# -----------------------------------------------------------------------------
# Embed the Operations dashboard in the app
# DASHBOARD_EMBED_URL=

# Use mock data when SQL/warehouse is unavailable (true/false)
# USE_MOCK_DATA=false

# Delivery-risk model serving endpoint for What-if tab
# SERVING_ENDPOINT_NAME=

# -----------------------------------------------------------------------------
# Alerts job (notebooks/06_alerts_kpi.py) – set in job env or here for local run
# -----------------------------------------------------------------------------
# DEMO_APP_URL=https://adb-984752964297111.11.azuredatabricks.net/apps/...
# DEMO_GENIE_URL=https://adb-984752964297111.11.azuredatabricks.net/genie

# -----------------------------------------------------------------------------
# Genie deploy script (scripts/deploy_genie_space.py) – for deploy only
# -----------------------------------------------------------------------------
# DATABRICKS_TOKEN=...
# GENIE_WAREHOUSE_ID=   (or script infers from DATABRICKS_HTTP_PATH)
# GENIE_PARENT_PATH=/Shared
# DEMO_CATALOG=welch
# DEMO_SCHEMA=denso_ai

# -----------------------------------------------------------------------------
# Bundle (databricks.yml) – usually set in bundle vars or CI, not .env
# -----------------------------------------------------------------------------
# catalog, schema, warehouse_id, app_name – see databricks.yml variables.
# warehouse_id for dashboard/orchestration is set in databricks.yml under targets.dev.variables.
